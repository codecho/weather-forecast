#配置Druid数据源
spring:
  datasource:
    username: root
    password: 1741248769
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/android?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&useSSL=false
    type: com.alibaba.druid.pool.DruidDataSource

#配置Mybatis
mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/*.xml

#配置tomcat端口和访问路径
server:
  port: 8080
  servlet:
    context-path: /user
